clear;
% k = 0.01/4.66;
% n = 0.22*sqrt(0.3*0.7/100);
% disp(n);

% errA = 60/200;
% errB = 30 / 120;
% zN = 1.96;
% d = errB - errA;
% rst1 = d - zN * sqrt(errA*(1-errA)/200 + errB*(1-errB)/120);
% rst2 = d + zN * sqrt(errA*(1-errA)/200 + errB*(1-errB)/120);
% zN2 = 1.64;
% rst3 = 0.05/sqrt(errA*(1-errA)/200 + errB*(1-errB)/120);

% p = 33/90;
% rst = p + 1.96 * sqrt(p*(1-p)/90);

% point = load('HW8_Problem2.txt');
% total = sum(point(:));
% l = length(point);
% % lamda = l/total;
% % rst = l * log(lamda)/log(exp(1)) - lamda * total;
% miu = total / l;
% zzz = point - miu;
% z = zzz .* zzz;
% rst = l * log(1/(2*sqrt(2*pi)))/log(exp(1)) - sum(z(:))/8;



